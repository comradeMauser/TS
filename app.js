"use strict";
function sum(n, m, k, j) {
    var result = n + m;
    return bool ? j + result : 'no result';
}
var num1;
num1 = 10;
var num2 = 3.3;
var bool = true;
var str = "Result: ";
var run = sum(num1, num2, bool, str);
console.log(result);
